# Property Sandbox

Initial project structure for data ingestion and processing pipeline.

## Structure
- `docs/` project documentation
- `src/` source code for pipeline scripts
- `data/raw/` original downloaded data
- `data/interim/` cleaned intermediate data
- `data/processed/` final processed datasets
- `outputs/` reports and generated artifacts
